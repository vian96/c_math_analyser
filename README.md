# Simple math analyser

It creates a math tree from a string and does simplification. Currentrly algorithm can only understand rational expressions and simplifies expressions like this: "(x+1)+2" => "x+1+2"
